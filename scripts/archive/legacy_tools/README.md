# Legacy Tools

These scripts were moved out of the top-level `scripts/` directory because they
are no longer the main workflow entrypoints. Keep them only for historical
reference or occasional spot checks.
